# 2026-03-25 日志

## 00:20（03-26）— 新增 3 个协议（SMTP / WebSocket / SSH）+ 清理重复数据问题

**每个协议完成内容**：PROTO_META 条目 + protocolDB 完整数据（rows/steps/stepData/knowledge/quiz）+ 协议库卡片改为可点击

- **SMTP（6步）**：TCP连接→EHLO协商→STARTTLS+AUTH→MAIL FROM/RCPT TO→DATA传邮件→QUIT；含信封vs邮件头、SPF/DKIM/DMARC；4道思考题
- **WebSocket（5步）**：HTTP Upgrade握手→101切换→帧传输（含Mask原理）→服务器推送（全双工）→Close帧关闭；4道思考题
- **SSH（6步）**：TCP连接+版本协商→算法协商→DH密钥交换（PFS）→公钥/密码认证→Channel会话→总结；含端口转发、known_hosts；4道思考题

**文件大小**：283,292 字节（+约3万字节）

**清理**：删除了之前意外产生的两份重复 smtp/websocket 数据块（共426行）

**当前已完成协议**（共12个）：TCP三握、TCP四挥、HTTP、TLS、DNS、UDP、ARP、ICMP、DHCP、**SMTP**、**WebSocket**、**SSH**

**待开发协议**（共10个）：NAT、VLAN、FTP、IP路由、TCP拥塞控制、HTTP/2、QUIC/HTTP3、OSPF、BGP、及其他

## 21:08 — 新增 3 个协议（ARP / ICMP / DHCP）

**每个协议完成内容**：PROTO_META 条目 + protocolDB 完整数据（rows/steps/stepData/knowledge/quiz）+ 协议库卡片改为可点击

- **ARP（5步）**：广播请求→单播回复→封装发送→缓存命中→总结；含ARP欺骗、免费ARP、IPv6 NDP对比；5道思考题
- **ICMP（5步）**：Ping Echo Request→Echo Reply→Traceroute TTL探针→Time Exceeded→Unreachable总结；5道思考题
- **DHCP（4步，DORA流程）**：Discover→Offer→Request→ACK；含T1/T2续约机制、DHCP中继；4道思考题

**文件大小**：253,442 字节（+约4万字节）

**当前已完成协议**（共9个）：TCP三次握手、TCP四次挥手、HTTP、TLS、DNS、UDP、**ARP**、**ICMP**、**DHCP**

**待开发协议**（共13个）：SMTP、WebSocket、NAT、VLAN、SSH、FTP、IP路由、TCP拥塞控制、HTTP/2、QUIC/HTTP3、OSPF、BGP，以及协议库里的其他卡片

## 20:00 — 全面 Bug 检查 & 修复（4项）

**检查范围**：协议卡片、PROTO_META、protocolDB、通用画布、switchProtocol、renderStep

**修复的4个Bug：**

1. **知识点/思考题残留问题（高）**：`renderStep` 中当某步骤无对应 knowledge/quiz 数据时，原来不会隐藏块，导致残留上一步内容。修复：无数据时加 `style.display='none'`，有数据时恢复显示。影响协议：http（5步但知识点/题只有3/2条）、tls（4步但3/2条）、dns（8步但3/2条）、udp（4步但quiz只有1条）。

2. **UDP异常场景按钮（中）**：`switchProtocol` 对所有协议都显示异常场景按钮，但 UDP 没有 `abnormal` 数据，点击后弹窗为空。修复：改为只有 `PROTO_META[pid].abnormal` 存在时才显示按钮。

3. **首页卡片步骤数错误（低）**：TLS 卡片写"12步"（实际4步）、HTTP 写"6步"（实际5步）、TCP四次挥手写"10步"（实际4步），同步修正了协议库里的TCP四次挥手（从10步→4步）。

4. **已确认无误**：协议库传输层 UDP 卡片早已存在且可点击（之前的记录遗漏了此项）。

## 09:09 — 网络协议可视化项目启动讨论

- 用户提出做网络协议可视化动画网站/App的想法
- 完成市场可行性分析：市场缺口真实，竞品薄弱，建议做
- 完成产品功能规划（三期）：MVP核心协议动画 → 交互沙盒 → 商业化
- 完成技术选型：Next.js + GSAP + React Flow + Supabase + Vercel
- 提出JSON脚本化动画数据结构设计（内容与渲染分离）
- 给出4周MVP落地路线图
- 用户下一步待定：可能直接开始写代码原型

## 09:25 — 竞品深度调研完成

调研对象：ChatTCP、传输层可视化模拟器、Networking Animations、Visual TCP/UDP Animator、Packet Tracer、国内MOOC平台、VisuAlgo、HowDNS.works、ByteByteGo
核心结论：直接竞品弱且窄（ChatTCP仅TCP，无步进），参考标杆（VisuAlgo）已验证产品模式价值，网络协议领域完全空白
差异化方向：广度（多协议）+ 步进控制 + 中文 + 持续运营

## 09:35 — UI原型设计完成

文件路径：C:\Users\wangjl\Desktop\20260318\neetwork\ui-prototype.html
包含页面：首页Landing、协议库列表页、TCP播放器页（含步进控制+字段解析+相关协议三个Tab）
设计风格：暗色科技风（区别于竞品ChatTCP的白底），深蓝/青色/紫色多彩协议标识
本地预览：http://127.0.0.1:8765/ui-prototype.html（需开启服务器）

## 09:50 — P0功能交互修复完成

完成四项P0级功能：
1. **协议库筛选逻辑**：给所有卡片加 data-layer/data-level/data-title，点击标签实时过滤；全部隐藏时显示空结果提示
2. **搜索框实时过滤**：input事件 + Esc清空，与筛选标签联动
3. **步骤卡片联动**：renderStep()同步更新右侧面板步骤卡片的 active/done 状态，并 scrollIntoView
4. **字段解析联动**：每步有独立的字段数组，步进时动态替换 tbody 内容
5. **横幅完成态**：第3步变为绿色"已建立"样式
6. **Toast通知**：分享按钮复制链接后提示、播放完成提示
7. **异常场景按钮**：点击显示"开发中"提示而非无响应

## 16:10 — 协议库卡片全面补齐（22+ 张）

**本次修改：**
1. 首页 TCP 拥塞控制卡：旧内联 `opacity:0.7;cursor:default` → `.coming-soon` 统一样式
2. 协议库应用层新增 4 张"即将上线"卡：SMTP、FTP、QUIC/HTTP3、SSH
3. 协议库网络/链路层新增 4 张"即将上线"卡：ICMP/Ping、NAT、BGP、VLAN
4. CARD_META 数组同步更新（应用层 10 条、传输层 4 条、网络/链路层 7 条）
5. 首页统计数字 20+ → 22+，协议库页头描述同步更新

**最终协议库结构：**
- 应用层：DNS、HTTP/1.1、TLS 1.3（可用）+ HTTP/2、DHCP、WebSocket、SMTP、FTP、QUIC、SSH（即将上线）= 10 张
- 传输层：TCP三次握手、TCP四次挥手（可用）+ TCP拥塞控制、UDP（即将上线）= 4 张
- 网络/链路层：IP路由、ARP、OSPF、ICMP、NAT、BGP、VLAN（全部即将上线）= 7 张
- 合计：22 张，筛选功能全部可用

## 16:02 — 修复协议库状态、自定义参数栏、异常场景弹窗

**问题三件套全部修复：**

1. **协议库 "即将上线" 卡片**：HTTP/2、DHCP、WebSocket、TCP拥塞控制、UDP、IP路由、ARP、OSPF 全部加 `.coming-soon` 样式（灰化 + 禁用悬停 + cursor:not-allowed）

2. **自定义参数栏动态显隐**：
   - 原因：HTML 中 `custom-seq-bar` 没有 id，JS 里找 `customSeqPanel` 永远找不到
   - 修复：给 `<div class="custom-seq-bar">` 加 `id="customSeqPanel"`
   - 效果：只在 tcp3 时显示 ISN 自定义栏，切换其他协议时自动隐藏

3. **异常场景弹窗按协议切换**：
   - 新增 `PROTO_META` 对象，包含每个协议的 title / layer / abnormal 场景数据（5条步骤 + tip）
   - 新增 `updateAbnormalOverlay(pid)` 函数，动态渲染弹窗 HTML
   - `switchProtocol()` 切换时自动关闭并更新弹窗内容
   - 页面初始化时调用 `updateAbnormalOverlay('tcp3')`
   - 覆盖：TCP三次握手(SYN-ACK丢包)、TCP四次挥手(ACK丢包)、HTTP(503)、TLS(证书验证失败)、DNS(NXDOMAIN)

## 15:49 — 修复协议卡片跳转 Bug

**问题根因**：协议库页面（page-protocols）和首页的 DNS/HTTP/TLS/TCP4 卡片都没有 onclick，或写的是 `showPage('player')` 而不带协议参数。

**修复方案**：
- 新增 `openProtocol(pid)` 工具函数：先 showPage('player')，延迟 50ms 后调 switchProtocol(pid)
- 首页 6 张卡片：tcp3/tls/dns/http/tcp4 补上 `openProtocol`；拥塞控制（无数据）标记为"即将上线"
- 协议库页（page-protocols）：dns/http/tls/tcp4 全部补上 `openProtocol`

## 15:33 — 四个协议完整数据实装完成

新增 buildProtocolData(pid) 函数，为 tcp4/http/tls/dns 各补全：
- 步骤卡片（steps）
- 每步渲染数据（stepData：字段表、旁白、banner、节点状态）
- 知识点块（knowledge：每步对应一条）
- 思考题（quiz：每步一题含答案）

**各协议步骤数量**：
- TCP 四次挥手：4步，4道题，4条知识点
- HTTP 请求响应：5步，5道题，5条知识点
- TLS 1.3 握手：4步，4道题，4条知识点
- DNS 解析：8步，5道题，4条知识点

同时修复初始化逻辑（补全 rebuildStepDots + rebuildSidePanel），
修复 switchProtocol 对 tcp3 的处理（调 rebuildStepData 而非 buildProtocolData）。
index.html 已同步（3458行）。

## 15:17 — index.html 同步 + README 更新完成

- 将 ui-prototype.html（3022行/145KB，包含全部三层教学增强）完整复制为 index.html（正式发布版）
- 更新 README.md：补充 GitHub Pages 部署步骤、键盘快捷键说明、三层教学增强功能列表
- 本地服务器 http://127.0.0.1:8765/index.html 可预览
- 项目现已具备直接上传 GitHub 发布的条件

## 09:50 — UI设计细节深度优化完成

主要改进项：
1. **颜色系统重构**：从旧的 #3b82f6 升级到更饱和的 #4f8ef7 系，建立完整的9个色彩语义变量（blue/cyan/purple/green/orange/red + glow/subtle变体）
2. **背景深度层次**：新增 bg-0（最深）到 bg-card-2（卡片悬停）共5个背景层次
3. **Hero区视觉增强**：网格纹理背景（带mask渐变淡出）+ 更强的三组径向光晕，整体科技感明显提升
4. **统计栏**：分隔线改为细竖线，数字渐变更纯粹
5. **卡片升级**：顶部色条加发光（box-shadow），新增卡片内光晕（::after），hover时底部CTA文字颜色变化+间距增大
6. **动画画布**：深色背景+网格纹理+中心径向光晕，节点盒有立体阴影和彩色边框
7. **控制栏**：全部改为SVG图标（跨平台一致），进度点有scale动画，播放按钮按下变绿
8. **键盘支持**：方向键步进 + 空格键播放/暂停
9. **可访问性**：ARIA标签、role属性、focus-visible样式
