 protocolDB['quic'] = {
    steps:[
      { title:'第 1 步：QUIC 握手 — 1-RTT 建立连接', desc:'QUIC 基于 UDP，将 TLS 1.3 握手内嵌到传输层。客户端发送 Initial 包（含 ClientHello），服务端回 Initial + Handshake 包（含 ServerHello + 证书 + Finished），客户端确认后连接建立。整个过程只需 1 个 RTT，比 TCP+TLS 的 3 RTT 大幅缩短。', packet:'Client Initial → ClientHello\nServer Initial + Handshake\nClient Handshake Finished' },
      { title:'第 2 步：0-RTT 恢复 — 第一个数据包即携带请求', desc:'客户端若曾连接过该服务器，可保存"会话票据"（Session Ticket）。再次连接时，客户端在第一个 Initial 包中直接携带 0-RTT 数据（应用数据），不等握手完成即发送请求，延迟降至 0 RTT。', packet:'Client Initial → 0-RTT Data: GET /（无需等待服务端响应）' },
      { title:'第 3 步：QUIC 流 — 独立可靠，无队头阻塞', desc:'QUIC 原生支持多路复用流（Stream）。每个流独立维护序号和重传，一个流的包丢失不影响其他流的接收——这彻底解决了 HTTP/2 在 TCP 层的队头阻塞。', packet:'STREAM Frame Stream0/4/8（三个流完全独立，互不阻塞）' },
      { title:'第 4 步：QPACK 头部压缩', desc:'HTTP/3 使用 QPACK 压缩头部（替代 HTTP/2 的 HPACK）。HPACK 要求按序处理；QPACK 将动态表操作分离到独立控制流，主数据流可乱序接收。静态表扩展到 99 项，压缩效率与 HPACK 相当，且无乱序阻塞。', packet:'HEADERS Frame (QPACK encoded)\n:method: GET → static idx 17' },
      { title:'第 5 步：连接迁移 — 网络切换不断线', desc:'QUIC 用 64 位 Connection ID 标识连接，而非 TCP 的"四元组"。当用户从 WiFi 切换到 4G 时，IP 和端口变化，TCP 连接断开；QUIC 只需使用相同的 Connection ID 继续传输，连接无缝迁移。', packet:'NEW_CONNECTION_ID → PATH_CHALLENGE → PATH_RESPONSE' },
      { title:'第 6 步：ACK + 拥塞控制改进', desc:'QUIC 的 ACK 机制比 TCP 更精准：Packet Number 单调递增（即使重传也用新号），避免 TCP 的重传歧义；ACK Delay 字段准确报告处理延迟，RTT 估算更精确。拥塞控制插件化，可使用 CUBIC、BBR 或自定义算法。', packet:'ACK Frame: Largest=145, Delay=2ms, Ranges=[140-145],[130-138]' },
      { title:'第 7 步：HTTP/3 = HTTP over QUIC', desc:'HTTP/3 将 HTTP/2 的语义（方法、头部、正文）运行在 QUIC 之上，去掉了 HTTP/2 的帧类型前缀（因 QUIC 已在传输层处理多路复用）。合并了传输+加密+应用层，实现最优延迟。', packet:'HTTP/3 HEADERS Frame (QUIC Stream 0)\n→ 1-RTT 即可开始传输' },
    ],
    rows:[
      { dir:'r', color:'packet-label-cyan',   label:'Client Initial（ClientHello 内嵌 TLS 1.3）' },
      { dir:'r', color:'packet-label-blue',   label:'0-RTT Data: GET /（首包携带请求）' },
      { dir:'r', color:'packet-label-purple', label:'STREAM Frame Stream0/4/8（多路复用，无 HoL）' },
      { dir:'r', color:'packet-label-cyan',   label:'HEADERS Frame（QPACK 压缩头部）' },
      { dir:'r', color:'packet-label-green',  label:'NEW_CONNECTION_ID → PATH_CHALLENGE/RESPONSE' },
      { dir:'l', color:'packet-label-blue',   label:'ACK Frame（精准 RTT 采样）+ 拥塞窗口调整' },
      { dir:'r', color:'packet-label-green',  label:'HTTP/3 HEADERS + DATA（传输+加密+HTTP 合一）' },
    ],
    stepData:[
      {
        banner: '🚀 第 1 / 7 步 — QUIC 1-RTT 握手…',
        leftState: '发送 Initial',
        rightState: '等待 Initial',
        activeRow: 1,
        fieldTitle: 'QUIC Initial 包头字段',
        fields:[
          { name:'Header Form',               value:'Long Header (1)',      desc:'QUIC 包头分长（握手期）和短（数据期）两种格式，Initial 包用长头', highlight:true },
          { name:'Version',                   value:'0x00000001 (v1)',      desc:'QUIC 版本号，RFC 9000 为版本 1；服务端不支持时返回 Version Negotiation 包', highlight:true },
          { name:'Destination Connection ID', value:'8~20 字节随机',        desc:'客户端首次发送时随机生成，服务端响应后可更换为自己分配的 ID', highlight:true },
          { name:'Token',                     value:'空（首次连接）',        desc:'重连时携带服务端颁发的令牌，可跳过地址验证；首次连接为空', highlight:false },
          { name:'Packet Number',             value:'0（初始）',            desc:'QUIC 包号单调递增，握手包号空间独立于应用数据包号空间', highlight:false },
        ],
        narration:'QUIC 握手将 TLS 1.3 内嵌在传输层完成，Initial 包承载 ClientHello，Handshake 包承载证书和 Finished。整个握手 1 RTT 完成，而 TCP+TLS 1.3 至少需要 2 RTT（TCP 握手 1 RTT + TLS 1 RTT）。QUIC Initial 包用 AES-128-ECB 固定密钥加密（防止中间设备篡改，但任何人都能解密——真正的机密由 TLS 1.3 密钥保护）。',
      },
      {
        banner: '⚡ 第 2 / 7 步 — 0-RTT，第一个包携带请求！',
        leftState: '发送 0-RTT 数据',
        rightState: '处理 0-RTT',
        activeRow: 2,
        fieldTitle: '0-RTT 相关字段',
        fields:[
          { name:'Early Data',           value:'0-RTT STREAM Frame',  desc:'使用上次会话协商的密钥（PSK）加密，随 Initial 包一起发出，服务端解密后直接处理请求', highlight:true },
          { name:'Session Ticket',       value:'NST 令牌',              desc:'首次连接时服务端通过 NEW_SESSION_TICKET 帧颁发，客户端保存在本地（类似 Cookie）', highlight:true },
          { name:'max_early_data_size',  value:'通常 0xFFFFFFFF',      desc:'服务端在 Session Ticket 中声明允许多少字节的 0-RTT 数据', highlight:false },
          { name:'重放风险',              value:'仅限幂等请求',          desc:'0-RTT 数据可能被攻击者重放。浏览器限制只对 GET 等安全方法使用 0-RTT，POST 等必须等 1-RTT 确认', highlight:true },
        ],
        narration:'0-RTT 是 QUIC 最震撼的特性：用户再次访问曾访问过的网站，第一个数据包就携带了 HTTP 请求，服务端收到后无需等待握手完成即可开始处理。体感延迟接近 0。代价是 0-RTT 数据可被攻击者截取并重放（Forward Secrecy 降级），因此只对无副作用的请求开放。',
      },
      {
        banner: '🚀 第 3 / 7 步 — QUIC 流多路复用，彻底消除队头阻塞！',
        leftState: '并发请求中',
        rightState: '并发响应中',
        activeRow: 3,
        fieldTitle: 'QUIC STREAM 帧字段',
        fields:[
          { name:'Stream ID',   value:'0, 4, 8（偶数）',  desc:'客户端双向流 ID 为 4n+0（0,4,8...），服务端双向流为 4n+1，单向流另两类', highlight:true },
          { name:'Offset',      value:'字节偏移量',        desc:'流内数据偏移，用于重组乱序数据；独立于其他流，不同流的乱序互不影响', highlight:true },
          { name:'FIN bit',     value:'1=流结束',         desc:'类比 TCP FIN，置 1 表示本端该流已发完，流半关闭', highlight:false },
          { name:'vs HTTP/2',   value:'TCP 层无此能力',   desc:'HTTP/2 也有多路复用，但 TCP 层一个包丢失会阻塞所有流；QUIC 每个流独立重传，丢包只影响对应流', highlight:true },
        ],
        narration:'HTTP/2 的队头阻塞在 TCP 层根深蒂固：TCP 保证有序交付，一个包丢失，后续所有字节都被内核缓冲区挡住，所有 HTTP/2 流集体等待。QUIC 把有序保证下放到流级别，不同流的包可乱序到达，各自独立重传。实测在 1% 丢包率下，QUIC 的吞吐量比 HTTP/2-over-TCP 高出 30%+。',
      },
      {
        banner: '🚀 第 4 / 7 步 — QPACK 头部压缩',
        leftState: '压缩请求头',
        rightState: '解压头部',
        activeRow: 4,
        fieldTitle: 'QPACK 编解码机制',
        fields:[
          { name:'静态表',              value:'99 项（HTTP/3 扩展）',   desc:'在 HPACK 61 项基础上扩充了常见 HTTP/3 头部组合（如 :method GET + :scheme https）', highlight:true },
          { name:'编码器流',            value:'单向 QUIC Stream 2',    desc:'发送动态表插入指令；与数据流分离，乱序到达也不阻塞数据解码', highlight:true },
          { name:'解码器流',            value:'单向 QUIC Stream 3',    desc:'确认动态表指令已处理，防止编码器过度膨胀动态表', highlight:true },
          { name:'Required Insert Count', value:'字段块中携带',        desc:'每个 QPACK 字段块携带所需动态表插入数，解码器检查后决定立即解码还是等待', highlight:true },
          { name:'vs HPACK',           value:'无阻塞 vs 需按序',       desc:'HPACK 要求严格按帧顺序处理动态表，QPACK 通过分离控制流实现无阻塞动态表更新', highlight:false },
        ],
        narration:'HPACK 在 HTTP/2 上工作完美，但移植到 QUIC 有问题：QUIC 流可乱序到达，如果 Stream 4 的头部引用了 Stream 0 还未处理的动态表条目，Stream 4 就会被阻塞——这正是 QUIC 要消除的问题。QPACK 将动态表操作放到独立的控制流，数据流解码时如果动态表还没准备好，才等待控制流——大多数情况下静态表足够，完全无阻塞。',
      },
      {
        banner: '📶 第 5 / 7 步 — 连接迁移，WiFi 切 4G 不断线！',
        leftState: '网络切换中',
        rightState: '接受连接迁移',
        activeRow: 5,
        fieldTitle: '连接迁移帧字段',
        fields:[
          { name:'Connection ID',       value:'64 位标识符',             desc:'QUIC 连接的唯一标识，不绑定 IP 和端口；TCP 四元组变化即连接断开，而 QUIC 靠 Connection ID 维持连接', highlight:true },
          { name:'NEW_CONNECTION_ID',   value:'预先分配备用 ID',          desc:'服务端提前发送多个备用连接 ID，客户端网络切换时直接使用新 ID，无需额外交互', highlight:true },
          { name:'PATH_CHALLENGE',      value:'8字节随机数',               desc:'验证新网络路径的可达性，防止攻击者伪造迁移', highlight:true },
          { name:'PATH_RESPONSE',       value:'回显 PATH_CHALLENGE',      desc:'对端原样返回随机数，证明路径有效；之后正式切换到新路径传输', highlight:false },
          { name:'实际效果',             value:'语音/视频通话不中断',       desc:'移动端从 WiFi 走出楼宇切换到 5G，基于 QUIC 的应用（如 YouTube、Google Meet）不会重新缓冲', highlight:true },
        ],
        narration:'连接迁移是 QUIC 对移动场景的革命性改进。TCP 连接由"本地IP:端口 + 远端IP:端口"唯一确定，换了网络就是新的四元组，旧连接立即失效，必须重新建立（重新握手 + 重新缓冲）。QUIC 改为用 Connection ID 标识，只要 Connection ID 一致，服务端就认为是同一连接，网络路径切换透明无感。',
      },
      {
        banner: '🚀 第 6 / 7 步 — 精准 ACK + 可插拔拥塞控制',
        leftState: 'ACK 确认中',
        rightState: '拥塞控制中',
        activeRow: 6,
        fieldTitle: 'ACK 帧字段',
        fields:[
          { name:'Largest Acknowledged', value:'最大已确认包号',      desc:'已成功接收的最大包号，结合 ACK Ranges 描述所有已收包', highlight:true },
          { name:'ACK Delay',            value:'微秒级精度',           desc:'接收方从收到包到发出 ACK 的延迟，让发送方精确计算 RTT，避免 TCP 的 ACK 延迟估算误差', highlight:true },
          { name:'ACK Ranges',           value:'多段乱序确认',         desc:'可在单个 ACK 帧中确认多达 256 个不连续的包范围，减少 ACK 包数量', highlight:true },
          { name:'无重传歧义',            value:'新包号 ≠ 旧包号',       desc:'TCP 重传包与原始包序号相同，难以区分；QUIC 重传用全新包号，ACK 精准指向新包，RTT 采样无歧义', highlight:true },
          { name:'拥塞控制',             value:'CUBIC / BBR 可插拔',   desc:'QUIC 拥塞控制在用户空间实现，可运行时升级，无需等待内核更新。谷歌部署 BBR，带宽利用率提升 75%', highlight:false },
        ],
        narration:'TCP 的"重传歧义"是个经典问题：重传包与原始包序号相同，ACK 到来时无法判断确认的是哪个，导致 RTT 采样偏差。QUIC 每次重传都用新包号，彻底消除这个歧义。ACK Delay 字段让接收方主动汇报处理延迟，发送方可准确区分传播延迟和处理延迟，RTT 估算更精确，拥塞控制更激进、更高效。',
      },
      {
        banner: '✅ HTTP/3 连接就绪，最优延迟传输！',
        leftState: 'HTTP/3 就绪',
        rightState: 'HTTP/3 响应中',
        activeRow: 7,
        fieldTitle: 'HTTP/3 控制结构',
        fields:[
          { name:'HTTP/3 控制流',    value:'单向 Stream（0x00）',  desc:'每端各一条，传输 SETTINGS、GOAWAY 等控制帧；不可关闭', highlight:true },
          { name:'QPACK 编码器流',   value:'单向 Stream（0x02）',  desc:'发送 QPACK 动态表插入指令', highlight:false },
          { name:'QPACK 解码器流',   value:'单向 Stream（0x03）',  desc:'确认动态表条目已处理', highlight:false },
          { name:'请求流',           value:'双向 Stream（4,8,12...）', desc:'每个 HTTP/3 请求一条双向流；客户端发 HEADERS + DATA，服务端响应 HEADERS + DATA', highlight:true },
          { name:'Alt-Svc 升级',    value:'HTTP 响应头',           desc:'服务端通过 Alt-Svc: h3=":443" 告知客户端支持 HTTP/3；浏览器下次直接用 QUIC 连接', highlight:true },
        ],
        narration:'HTTP/3 把 HTTP 语义（方法/头部/正文）直接运行在 QUIC 之上，去掉了 HTTP/2 的 TCP 帧层，保留帧结构（但帧类型更精简）。首次访问网站仍通过 HTTPS（HTTP/1.1 或 HTTP/2），服务端通过 Alt-Svc 响应头声明支持 HTTP/3，浏览器记录下来，下次直接用 QUIC 连接。这也是为什么 HTTP/3 普及是渐进式的——不破坏兼容性。',
      },
    ],
    knowledge:[
      { title:'💡 QUIC 与 HTTP/3 的关系', body:'QUIC 是传输层协议（替代 TCP，基于 UDP），HTTP/3 是运行在 QUIC 上的应用层协议（替代 HTTP/2）。类比：TCP 之于 HTTP/2，QUIC 之于 HTTP/3。QUIC 本身也可以承载其他协议（如 WebTransport、DNS over QUIC），并非 HTTP 专属。' },
      { title:'💡 为什么 QUIC 基于 UDP 而不是 TCP？', body:'① <strong>消除 TCP 队头阻塞</strong>：TCP 保证全局有序，QUIC 只在流内保序；② <strong>握手加速</strong>：内嵌 TLS 1.3，1-RTT 建连，0-RTT 恢复；③ <strong>用户空间实现</strong>：TCP 在内核，升级慢；QUIC 在应用层，Chrome/Nginx 可快速迭代；④ <strong>连接迁移</strong>：Connection ID 替代四元组；⑤ <strong>UDP 部署广泛</strong>：防火墙和 NAT 普遍支持 UDP 443。' },
      { title:'💡 HTTP/1.1 → HTTP/2 → HTTP/3 演进对比', body:'<strong>HTTP/1.1</strong>：文本协议，每连接串行请求，无压缩，多连接并发（6~8 个）<br><strong>HTTP/2</strong>：二进制帧，单连接多路复用（消除应用层 HoL），HPACK 压缩，Server Push；但 TCP 层仍有 HoL<br><strong>HTTP/3</strong>：QUIC（UDP），彻底消除 HoL，0-RTT，QPACK，连接迁移，用户空间拥塞控制' },
      { title:'💡 QUIC 的现状与普及率', body:'截至 2025 年：Chrome、Firefox、Safari 全面支持；Nginx 1.25+、Caddy 2 原生支持 HTTP/3；CloudFlare、Google、Meta 已将大量流量迁移到 QUIC（Google 报告约 50% 流量走 QUIC）。HTTP/3 RFC 9114 于 2022 年发布，QUIC RFC 9000 同年发布。主要障碍：企业防火墙常封锁 UDP 443，导致部分环境回退 HTTP/2。' },
      { title:'💡 QUIC 连接迁移原理', body:'TCP 连接由"本地IP:端口 + 远端IP:端口"唯一确定，IP 或端口变化连接立即断开。QUIC 用 <strong>64 位 Connection ID</strong> 标识连接，网络切换时通过 NEW_CONNECTION_ID + PATH_CHALLENGE/PATH_RESPONSE 验证新路径可达性，切换过程在协议层自动完成，应用层无感知。移动端 WiFi 切 4G 时语音/视频不中断。' },
      { title:'💡 QUIC 精准 ACK 与无重传歧义', body:'TCP 重传包与原始包序号相同，ACK 到来时无法区分确认的是哪个（重传歧义），导致 RTT 采样偏差。QUIC 每次重传都用<strong>全新包号</strong>，彻底消除歧义。<strong>ACK Delay</strong> 字段让接收方主动汇报处理延迟，发送方精确区分传播延迟与处理延迟，拥塞控制（CUBIC/BBR 可插拔）更精准高效。' },
      { title:'💡 HTTP/3 如何被浏览器发现？', body:'首次访问仍走 TCP（HTTP/1.1 或 HTTP/2）。服务端在响应头中加入 <code>Alt-Svc: h3=":443"; ma=86400</code>，告知浏览器"我的 443 端口支持 HTTP/3，有效期 1 天"。浏览器记录后，<strong>下次访问</strong>直接尝试 QUIC 连接（与 TCP 竞速）。HTTP/3 保留 HTTP/2 的帧语义，但去掉了 TCP 帧层，将传输+加密+应用合为一体。' },
    ],
    quiz:[
      { q:'QUIC 如何在 UDP 上实现可靠传输？为什么不直接用 TCP？', a:'QUIC 在 UDP 之上自行实现可靠传输：每个 QUIC 包有唯一递增的 Packet Number，接收方通过 ACK 帧确认；未被确认的包触发重传。QUIC 不用 TCP 的原因：① TCP 在内核实现，修改慢；② TCP 队头阻塞无法在协议层绕开；③ TCP 握手无法与 TLS 合并为 1 RTT；④ TCP 连接与 IP:端口绑定，无法连接迁移。' },
      { q:'0-RTT 的重放攻击是怎么回事？如何防御？', a:'0-RTT 数据在 TLS 完整握手完成前发送，攻击者可截获并重放这些数据包，让服务端重复执行请求（如重复下单）。防御措施：① 服务端只对<strong>幂等请求</strong>（GET、HEAD）接受 0-RTT；② 服务端维护"已使用 0-RTT 令牌"列表，拒绝重复令牌；③ 为 0-RTT 数据设置时间窗口。' },
      { q:'HTTP/3 如何被浏览器发现并使用？首次访问仍需 TCP 吗？', a:'是的，首次访问仍走 TCP（HTTP/1.1 或 HTTP/2）。服务端在响应头中加入 <code>Alt-Svc: h3=":443"; ma=86400</code>，告知浏览器"我的 443 端口支持 HTTP/3（QUIC），有效期 1 天"。浏览器记录此信息，<strong>下次访问</strong>同一域名时直接尝试 QUIC 连接（与 TCP 竞速，QUIC 赢则用 HTTP/3）。' },
      { q:'QUIC 连接迁移和 TCP 连接中断恢复有什么本质区别？', a:'TCP 没有原生连接迁移能力。IP 或端口变化意味着新的四元组，旧连接立即失效，应用层必须感知断线并重新连接（重新握手，约 100~300ms 延迟）。QUIC 用 Connection ID 标识连接，IP 和端口变化后服务端通过 PATH_CHALLENGE/PATH_RESPONSE 验证新路径可达性，整个切换在协议层完成，<strong>应用层无感知</strong>，传输中的数据流不中断。' },
    ],
  };
