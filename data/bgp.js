protocolDB['bgp'] = {
  rows: [
    { dir:'c2s', color:'packet-label-blue',   label:'TCP SYN → SYN-ACK（端口 179，三次握手）' },
    { dir:'c2s', color:'packet-label-purple', label:'OPEN 报文（AS 号 / BGP 版本 / Hold Time）' },
    { dir:'s2c', color:'packet-label-purple', label:'OPEN 报文（对端 AS 号）+ KEEPALIVE 确认' },
    { dir:'c2s', color:'packet-label-cyan',   label:'UPDATE 报文（NLRI 路由前缀 + Path Attributes）' },
    { dir:'s2c', color:'packet-label-cyan',   label:'UPDATE 报文（对端路由前缀）' },
    { dir:'s2c', color:'packet-label-green',  label:'KEEPALIVE（定期心跳，维持邻居关系）' },
  ],
  steps: [
    { title:'TCP 建立会话（端口 179）', emoji:'🔌' },
    { title:'OPEN 报文交换，协商 BGP 参数', emoji:'🤝' },
    { title:'KEEPALIVE 确认，进入 Established', emoji:'✅' },
    { title:'UPDATE 发送本 AS 路由前缀', emoji:'📤' },
    { title:'UPDATE 接收对端路由，BGP 路由决策', emoji:'🧮' },
    { title:'KEEPALIVE 维持邻居 + 路由撤销机制', emoji:'💓' },
  ],
  stepData: [
    {
      banner:'🔌 第 1 / 6 步 — BGP 运行在 TCP 之上，首先建立 TCP 连接（端口 179）',
      leftState:'AS 65001（R1）', rightState:'AS 65002（R2）',
      fields:[
        { name:'协议定位',   value:'BGP-4（RFC 4271），当前互联网唯一 EGP（外部网关协议）', desc:'IGP（如 OSPF/EIGRP）管理 AS 内部路由；EGP（BGP）管理 AS 间路由，是互联网的"粘合剂"' },
        { name:'TCP 端口',   value:'179（BGP 专用端口）', desc:'BGP 利用 TCP 的可靠传输，自身不需要实现确认/重传机制；连接时主动方端口随机，被动方固定 179' },
        { name:'AS 号',      value:'AS 65001（私有）/ AS 65002（私有），公有 AS 号由 IANA 分配', desc:'16 位 AS 号范围 1-65535，64512-65534 为私有；32 位扩展 AS 号可支持更大规模' },
        { name:'eBGP vs iBGP', value:'eBGP：不同 AS 间；iBGP：同一 AS 内部', desc:'eBGP 邻居通常直连（TTL=1），可用 ebgp-multihop 扩展；iBGP 需全互联或用路由反射器' },
        { name:'BGP 邻居配置', value:'手动指定（非自动发现），必须明确配置 neighbor IP + remote-as', desc:'BGP 没有 Hello 自动发现机制，每条对等关系都需要两端手动配置' },
      ],
      narration:'BGP 和 OSPF 最大的不同：OSPF 在同一 AS 内自动发现邻居，BGP 要手动配置。BGP 运行在 TCP 179 端口上，天然具备可靠传输。互联网上每个运营商（AS）通过 BGP 互联，构成了全球路由表——目前约 90 万条前缀。',
    },
    {
      banner:'🤝 第 2 / 6 步 — TCP 建立后，双方交换 OPEN 报文，协商 BGP 会话参数',
      leftState:'Connect 状态', rightState:'Active 状态',
      fields:[
        { name:'BGP 状态机', value:'Idle → Connect → Active → OpenSent → OpenConfirm → Established', desc:'6 个状态，Active 表示正在尝试 TCP 连接；Established 为正常工作状态' },
        { name:'OPEN 报文字段', value:'Version（4）/ My AS / Hold Time / BGP Identifier（Router ID）/ Optional Parameters', desc:'BGP Version 目前固定为 4；BGP Identifier 通常选最大 Loopback IP' },
        { name:'Hold Time',  value:'协商取双方较小值，默认 90 秒，最小值 3 秒', desc:'Hold Time 为 0 表示不使用 Keepalive；Hold Time / 3 = Keepalive 发送间隔' },
        { name:'Capability', value:'OPEN 的可选参数里携带能力协商（Capability Negotiation）', desc:'如：多协议扩展（IPv6/VPN）、Route Refresh、4 字节 AS 号、Graceful Restart 等' },
        { name:'参数不匹配', value:'任何参数不兼容时发送 NOTIFICATION 报文，连接终止', desc:'常见错误码：Unsupported Version Number / Bad Peer AS / Unsupported Capability' },
      ],
      narration:'OPEN 报文是 BGP 的"自我介绍"——我是哪个 AS，我的 Router ID 是什么，我能支持哪些功能。两端参数匹配后才能进入下一步。这里最容易出的问题：AS 号配错、Loopback 不通（iBGP）、TTL 不够（多跳 eBGP 忘开 multihop）。',
    },
    {
      banner:'✅ 第 3 / 6 步 — 双方互发 KEEPALIVE 确认 OPEN 接受，进入 Established 状态',
      leftState:'OpenConfirm → Established', rightState:'OpenConfirm → Established',
      fields:[
        { name:'KEEPALIVE 报文', value:'最简单的 BGP 报文，仅 19 字节（固定头部），无载荷', desc:'作用一：确认 OPEN 接受；作用二：后续定期发送，维持 Hold Time 计时器' },
        { name:'Established 状态', value:'BGP 会话正式建立，可以开始交换路由前缀', desc:'只有到达 Established，BGP 才开始收发 UPDATE 报文' },
        { name:'Keepalive 间隔', value:'Hold Time / 3（默认 30 秒），Hold Time 超时则认为邻居失联', desc:'Keepalive 是心跳包；Hold Time 是"最长等待时间"，超时清除所有该邻居的路由' },
        { name:'BGP 报文类型', value:'OPEN / KEEPALIVE / UPDATE / NOTIFICATION（4 种）', desc:'NOTIFICATION 用于报告错误，发送后立即关闭 TCP 连接' },
        { name:'会话重置', value:'NOTIFICATION 或 TCP 断开 → 回到 Idle 状态，按 Connect Retry Timer 重连', desc:'生产网络中频繁 BGP Flap 会导致大量 UPDATE，需配置 route dampening 和 BFD' },
      ],
      narration:'Established 是 BGP 的"婚约"。此后 BGP 邻居每隔 30 秒互发 Keepalive 心跳，若 90 秒内收不到任何报文，认为邻居失联，撤销其所有路由——这就是 BGP Hold Time。对于核心路由器，一条 BGP 会话断开可能影响数百万用户的路由。',
    },
    {
      banner:'📤 第 4 / 6 步 — R1 发送 UPDATE 报文，向 R2 通告本 AS 内的可达路由前缀',
      leftState:'发送 UPDATE', rightState:'接收并处理',
      fields:[
        { name:'UPDATE 报文结构', value:'Withdrawn Routes（撤销路由）+ Path Attributes（路径属性）+ NLRI（新路由前缀）', desc:'一条 UPDATE 可以同时携带撤销路由和新路由，也可只含其中一种' },
        { name:'NLRI',           value:'Network Layer Reachability Information，IP 前缀列表（如 192.0.2.0/24）', desc:'每条 NLRI 共享同一组 Path Attributes，属性相同的前缀打包在一条 UPDATE 里' },
        { name:'NEXT_HOP',       value:'eBGP：下一跳设为本接口 IP；iBGP：保留 eBGP 学到的原始下一跳', desc:'iBGP 不修改 NEXT_HOP，需要 IGP 确保下一跳可达，否则路由不可用' },
        { name:'AS_PATH',        value:'经过的 AS 号列表（如 AS_SEQUENCE: 65001）', desc:'eBGP 转发时追加本 AS 号；若收到包含自身 AS 号的 UPDATE，则丢弃（防环）' },
        { name:'LOCAL_PREF',     value:'仅在 iBGP 内传递，默认 100，越高越优先', desc:'用于 AS 内统一出口策略：哪条 eBGP 链路是首选出口' },
        { name:'MED',            value:'Multi-Exit Discriminator，告知对端 AS 进入本 AS 的首选入口', desc:'只在相邻 AS 间传递，不传给第三个 AS；越小越优先（默认 0）' },
      ],
      narration:'UPDATE 是 BGP 的"核心货物"。Path Attributes 是 BGP 路由策略的关键——运营商通过调整 AS_PATH 长度、LOCAL_PREF、MED 等属性，精确控制流量走哪条路。BGP 不追求最快路径，而是追求"可控路径"——这是互联网商业逻辑的体现。',
    },
    {
      banner:'🧮 第 5 / 6 步 — R2 收到 UPDATE，运行 BGP 路由决策算法，选出最优路径',
      leftState:'路由表更新', rightState:'BGP 决策流程',
      fields:[
        { name:'BGP 路由决策顺序（从高到低）', value:'① WEIGHT（Cisco 私有，本地有效）② LOCAL_PREF ③ 本地起源 ④ AS_PATH 最短 ⑤ ORIGIN（IGP > EGP > ?）⑥ MED 最小 ⑦ eBGP > iBGP ⑧ IGP Metric 最小 ⑨ Router ID 最小', desc:'依次比较，先满足高优先级规则即胜出，无需继续比较' },
        { name:'WEIGHT', value:'Cisco 私有属性，范围 0-65535，默认 32768（本地起源）/ 0（邻居学到）', desc:'仅在本路由器有效，不传给邻居；最高优先级，常用于单台路由器的出口控制' },
        { name:'LOCAL_PREF',   value:'AS 内所有路由器共享，默认 100', desc:'最简洁的出口流量控制手段：边界路由器收到路由时设置，iBGP 全 AS 传播' },
        { name:'AS_PATH 防环', value:'eBGP 转发时追加本 AS 号；收到含自身 AS 号的路由则丢弃', desc:'这是 BGP 的基本防环机制——路由不会原路返回' },
        { name:'路由安装',     value:'最优路径安装到 RIB（路由信息库），再 redistribute 到 FIB 转发', desc:'BGP 路由需要递归查找下一跳（通过 IGP 解析），NEXT_HOP 必须可达' },
        { name:'Route Reflector', value:'解决 iBGP 全互联问题：RR 向所有 Client 反射 BGP 路由', desc:'替代 iBGP 全互联（n*(n-1)/2 会话）；大型 AS 常用层次化 RR 架构' },
      ],
      narration:'BGP 路由决策的顺序是固定的，记住前几条就够用了：Weight > LOCAL_PREF > AS_PATH 长度 > MED。实际运营中 90% 的流量工程靠 LOCAL_PREF（控制出口）和 AS_PATH 预置（控制入口）就能解决。BGP 的灵活性让它成为互联网流量工程的基础设施。',
    },
    {
      banner:'💓 第 6 / 6 步 — KEEPALIVE 维持会话心跳 + WITHDRAW 路由撤销机制',
      leftState:'Established（持续）', rightState:'Established（持续）',
      fields:[
        { name:'KEEPALIVE 心跳', value:'每 30 秒发一次，Hold Time 90 秒内无包则清除邻居路由', desc:'Hold Time = 0 时关闭 Keepalive 机制（不推荐，依赖 TCP 检测故障）' },
        { name:'UPDATE WITHDRAW', value:'路由撤销：UPDATE 报文的 Withdrawn Routes 字段携带被撤销的前缀', desc:'路由器宕机时 TCP 断开，BGP 自动撤销所有该邻居路由；接口 down 则发 WITHDRAW' },
        { name:'Route Dampening', value:'抑制频繁震荡（Flap）的路由：每次 Flap 累积惩罚值，超过阈值则抑制', desc:'防止不稳定路由频繁更新冲击全网；抑制期间不传播该路由' },
        { name:'BFD 联动',       value:'BFD（双向转发检测）提供毫秒级故障检测，触发 BGP 快速收敛', desc:'BGP 自身检测故障最慢需 90 秒（Hold Time），BFD 可缩短到 300ms' },
        { name:'Graceful Restart', value:'设备重启时通知邻居保留转发表，BGP 会话重建后恢复路由', desc:'避免重启导致的路由撤销/重建带来的流量中断，核心路由器必备特性' },
        { name:'BGP 4 字节 AS', value:'RFC 6793，将 AS 号从 16 位扩展到 32 位（约 42 亿），解决 AS 号耗尽', desc:'新 AS 号范围如 131072（4字节），与旧设备兼容时用 AS_TRANS = 23456' },
      ],
      narration:'BGP 的可靠性设计：Keepalive 检测邻居存活，WITHDRAW 通知路由失效，Graceful Restart 保证重启不断流，Route Dampening 防止抖动扩散。这套机制共同维护了互联网路由的稳定性。现实中一次大型 BGP 配置错误（如 2010 年中国电信泄露路由）可以在 18 分钟内影响全球 15% 的互联网流量。',
    },
  ],
  knowledge: [
    { label:'BGP 基本定位',   value:'唯一 EGP，管理 AS 间路由；运行在 TCP 179 端口；手动配置邻居（无自动发现）；eBGP 不同 AS，iBGP 同 AS 内部' },
    { label:'BGP 状态机',     value:'Idle → Connect → Active → OpenSent → OpenConfirm → Established（6 个状态，Established 为正常工作状态）' },
    { label:'4 种报文',       value:'OPEN（协商参数）/ KEEPALIVE（心跳）/ UPDATE（路由通告/撤销）/ NOTIFICATION（错误通知，触发后立即关闭 TCP）' },
    { label:'核心 Path Attributes', value:'AS_PATH（防环+路径选择）/ NEXT_HOP / LOCAL_PREF（AS 内出口控制）/ MED（AS 间入口建议）/ ORIGIN' },
    { label:'路由决策顺序',   value:'WEIGHT > LOCAL_PREF > 本地起源 > AS_PATH 最短 > ORIGIN > MED 最小 > eBGP > iBGP > IGP Metric > Router ID' },
    { label:'BGP 高可用特性', value:'Route Dampening（抑制震荡）/ BFD 联动（毫秒级检测）/ Graceful Restart（重启不断流）/ Route Reflector（解决 iBGP 全互联）' },
  ],
  quiz: [
    { q:'BGP 运行在哪个传输层协议和端口之上？',
      options:['UDP 520','TCP 179','TCP 389','UDP 161'],
      answer: 1,
      exp:'BGP 使用 TCP 179 端口，利用 TCP 提供的可靠传输。这也是 BGP 不需要自己实现确认/重传的原因。对比：RIP 用 UDP 520，SNMP 用 UDP 161，LDAP 用 TCP/UDP 389。' },
    { q:'BGP 会话进入哪个状态后才能开始交换 UPDATE 路由信息？',
      options:['Active','OpenSent','OpenConfirm','Established'],
      answer: 3,
      exp:'只有进入 Established 状态，BGP 对等体关系才正式建立，双方才开始交换 UPDATE 报文。Active 状态表示正在尝试 TCP 连接，OpenSent/OpenConfirm 是 OPEN 报文交换阶段。' },
    { q:'eBGP 与 iBGP 的核心区别是？',
      options:['使用端口不同','eBGP 在不同 AS 之间，iBGP 在同一 AS 内部','eBGP 使用 UDP，iBGP 使用 TCP','iBGP 会修改 NEXT_HOP，eBGP 不修改'],
      answer: 1,
      exp:'eBGP（External BGP）用于不同 AS 之间；iBGP（Internal BGP）用于同一 AS 内部的路由器之间。注意：iBGP 不修改 NEXT_HOP（保留 eBGP 的原始下一跳），需要 IGP 确保 NEXT_HOP 可达。' },
    { q:'BGP 路由决策中，LOCAL_PREF 的作用是什么？',
      options:['控制进入本 AS 的流量入口','控制离开本 AS 的流量出口','计算 AS 间路径的跳数','防止路由环路'],
      answer: 1,
      exp:'LOCAL_PREF 越高越优先，用于控制本 AS 出口流量走哪条链路（出口策略）。它在整个 AS 内通过 iBGP 传播。MED 用于影响对端 AS 进入本 AS 的入口选择（入口策略）。' },
    { q:'BGP AS_PATH 属性的防环机制是如何工作的？',
      options:['设置 TTL=1，限制传播跳数','路由器收到包含自身 AS 号的路由时丢弃','使用 Hold Time 定时器超时','通过 NOTIFICATION 报文通知环路'],
      answer: 1,
      exp:'eBGP 路由器在转发 UPDATE 时将自身 AS 号追加到 AS_PATH。当一台路由器收到 UPDATE，发现 AS_PATH 中已包含自己的 AS 号，则说明产生了路由环路，直接丢弃该路由。这是 BGP 的基本防环机制。' },
  ],
};
