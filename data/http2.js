 protocolDB['http2'] = {
    steps:[
      { title:'第 1 步：TLS + ALPN 协商 HTTP/2', desc:'HTTP/2 必须在 TLS 上运行（h2）。客户端在 TLS ClientHello 中通过 ALPN 扩展声明支持 "h2"，服务端在 ServerHello 中确认，握手完成后即进入 HTTP/2 帧传输模式。', packet:'TLS ClientHello → ALPN: h2' },
      { title:'第 2 步：连接前置（SETTINGS 帧）', desc:'TLS 建立后，双方互发 HTTP/2 魔法字符串（PRI * HTTP/2.0...）和 SETTINGS 帧，协商关键参数：最大并发流数、初始窗口大小、最大帧大小、头部压缩表大小等。', packet:'Magic + SETTINGS Frame' },
      { title:'第 3 步：多路复用 — 多个请求并发', desc:'HTTP/2 将每个请求/响应分配到一个独立的"流"（Stream），每个流有唯一的奇数 ID（客户端发起）。多个流并发在同一 TCP 连接上传输，帧可交错发送，彻底解决 HTTP/1.1 的队头阻塞。', packet:'Stream 1/3/5: HEADERS Frame 并发' },
      { title:'第 4 步：HPACK 头部压缩', desc:'HTTP/2 使用 HPACK 算法压缩头部：① 静态表（61个常见头部）；② 动态表（本次会话中出现过的头部，编码为索引）；③ Huffman 编码。重复头部只传索引号，显著减少传输量。', packet:'HEADERS Frame — HPACK 压缩（压缩率 85%+）' },
      { title:'第 5 步：服务端推送（Server Push）', desc:'服务端在客户端请求 HTML 之前，主动推送它预判客户端需要的资源（如 CSS、JS）。通过 PUSH_PROMISE 帧通知客户端将要推送的资源，再通过对应 Stream 发送数据，减少一个 RTT。', packet:'PUSH_PROMISE Frame (Stream 2: /style.css)' },
      { title:'第 6 步：流控与优先级', desc:'HTTP/2 每个流和整个连接都有独立的流量控制窗口（WINDOW_UPDATE 帧）。客户端可通过 PRIORITY 帧声明流的优先级（依赖树+权重），指导服务端优先传输关键资源。', packet:'WINDOW_UPDATE + PRIORITY + RST_STREAM' },
    ],
    rows:[
      { dir:'r', color:'packet-label-cyan',   label:'TLS ClientHello (ALPN: h2)' },
      { dir:'l', color:'packet-label-blue',   label:'TLS ServerHello (ALPN: h2 确认) + Magic + SETTINGS' },
      { dir:'r', color:'packet-label-purple', label:'SETTINGS ACK + HEADERS Stream1/3/5（并发请求）' },
      { dir:'l', color:'packet-label-cyan',   label:'SETTINGS ACK + HPACK 压缩头部响应' },
      { dir:'l', color:'packet-label-green',  label:'PUSH_PROMISE Stream2 + DATA Stream1（HTML）' },
      { dir:'l', color:'packet-label-blue',   label:'DATA Stream2（CSS Push）+ WINDOW_UPDATE' },
    ],
    stepData:[
      {
        banner: '🔒 第 1 / 6 步 — TLS + ALPN 协商 HTTP/2…',
        leftState: 'TLS 握手中',
        rightState: '等待 ALPN',
        activeRow: 1,
        fieldTitle: 'TLS ALPN 扩展字段',
        fields:[
          { name:'Extension Type', value:'0x0010 (ALPN)', desc:'Application-Layer Protocol Negotiation，TLS 扩展，协商应用层协议', highlight:true },
          { name:'Protocol List',  value:'"h2", "http/1.1"', desc:'客户端按优先级声明支持的协议，h2 = HTTP/2 over TLS', highlight:true },
          { name:'ServerHello ALPN', value:'"h2"', desc:'服务端选择 h2，双方确认本次连接使用 HTTP/2', highlight:true },
          { name:'h2c', value:'HTTP/2 cleartext', desc:'HTTP/2 也支持明文（h2c），但浏览器均不支持，实际只用 h2（TLS）', highlight:false },
        ],
        narration:'HTTP/2 的 TLS 协商通过 ALPN 扩展完成，整个过程在 TLS 握手内发生，不增加额外 RTT。ALPN 取代了以前的 NPN（Next Protocol Negotiation）。浏览器访问 HTTPS 站点时，若服务器返回 ALPN "h2"，就自动升级到 HTTP/2，用户无感知。',
      },
      {
        banner: '🚀 第 2 / 6 步 — 连接前置，交换 SETTINGS…',
        leftState: '发送 SETTINGS',
        rightState: '发送 SETTINGS',
        activeRow: 2,
        fieldTitle: 'HTTP/2 SETTINGS 帧字段',
        fields:[
          { name:'魔法字符串', value:'PRI * HTTP/2.0\\r\\n\\r\\nSM\\r\\n\\r\\n', desc:'客户端发送的 24 字节前置序列，用于区分 HTTP/2 和 HTTP/1.1', highlight:true },
          { name:'HEADER_TABLE_SIZE', value:'4096', desc:'HPACK 动态表大小（字节），影响头部压缩效率', highlight:true },
          { name:'MAX_CONCURRENT_STREAMS', value:'100', desc:'允许对端同时打开的最大流数量', highlight:true },
          { name:'INITIAL_WINDOW_SIZE', value:'65535', desc:'流级别初始流量控制窗口（字节）', highlight:false },
          { name:'MAX_FRAME_SIZE', value:'16384', desc:'单帧最大负载（字节），可协商至 16777215', highlight:false },
          { name:'SETTINGS_ACK', value:'Flags=0x1', desc:'收到对端 SETTINGS 后必须回 ACK，否则对端等待超时', highlight:false },
        ],
        narration:'HTTP/2 连接建立后的"握手"：客户端发魔法字符串 + SETTINGS，服务端发 SETTINGS，双方各自 ACK。SETTINGS 帧（类型=0x4）在连接期间可随时发送来更新参数。这个过程仅发生一次，后续所有请求复用同一 TCP 连接。',
      },
      {
        banner: '🚀 第 3 / 6 步 — 多路复用，3个请求同时传输！',
        leftState: '并发请求中',
        rightState: '并发响应中',
        activeRow: 3,
        fieldTitle: 'HTTP/2 帧头字段（9字节固定）',
        fields:[
          { name:'Length',    value:'3字节', desc:'帧负载长度（不含9字节帧头）', highlight:false },
          { name:'Type',      value:'0x1 (HEADERS)', desc:'帧类型：HEADERS=0x1, DATA=0x0, SETTINGS=0x4, PUSH_PROMISE=0x5, WINDOW_UPDATE=0x8', highlight:true },
          { name:'Flags',     value:'0x4 (END_HEADERS)', desc:'标志位：END_STREAM=0x1（无更多数据）, END_HEADERS=0x4（头部完整）', highlight:true },
          { name:'Stream ID', value:'1, 3, 5', desc:'客户端发起的流 ID 为奇数（1,3,5...），服务端发起为偶数（2,4,6...）, 0=连接级', highlight:true },
          { name:'HTTP/1.1 对比', value:'队头阻塞', desc:'HTTP/1.1 每个连接同一时刻只能处理一个请求；HTTP/2 多路复用彻底消除这个瓶颈', highlight:false },
        ],
        narration:'多路复用是 HTTP/2 最核心的改进。HTTP/1.1 要并发请求需要打开 6~8 个 TCP 连接（浏览器限制），而 HTTP/2 只用 1 个连接，流可以无限并发（受 MAX_CONCURRENT_STREAMS 控制）。帧可以交错发送：Stream1-帧1、Stream3-帧1、Stream1-帧2……彻底消除应用层队头阻塞。',
      },
      {
        banner: '🚀 第 4 / 6 步 — HPACK 头部压缩，减少冗余传输',
        leftState: '发送请求头',
        rightState: '解压头部',
        activeRow: 4,
        fieldTitle: 'HPACK 压缩字段',
        fields:[
          { name:'静态表索引', value:':method GET → idx 2', desc:'RFC 7541 预定义 61 个常用头部索引，单字节即可表示常见头部', highlight:true },
          { name:'动态表',    value:'会话期间更新', desc:'会话中出现过的头部自动加入动态表，后续用索引替代，节省字节', highlight:true },
          { name:'Huffman 编码', value:'路径/值压缩', desc:'对字面量值进行 Huffman 编码，平均压缩 30%', highlight:true },
          { name:'压缩率',    value:'典型 85~90%', desc:'HTTP/1.1 请求头通常 500~800 字节，HPACK 后降至 50~100 字节', highlight:false },
          { name:'安全考虑', value:'CRIME/BREACH', desc:'HTTP/2 头部压缩不使用 TLS 压缩层，HPACK 针对压缩攻击做了专门设计', highlight:false },
        ],
        narration:'HTTP/1.1 中每个请求都要重复发送相同的 User-Agent、Accept、Cookie 等头部，单次请求头可达 500+ 字节。HPACK 通过静态表（61个常用头）+ 动态表（本次会话出现过的头）+ Huffman 编码，将头部体积压缩 85% 以上。移动网络下延迟改善尤为明显。',
      },
      {
        banner: '🚀 第 5 / 6 步 — 服务端推送，减少一个 RTT',
        leftState: '等待推送资源',
        rightState: '主动推送 CSS',
        activeRow: 5,
        fieldTitle: 'PUSH_PROMISE 帧字段',
        fields:[
          { name:'Promised Stream ID', value:'2（偶数）', desc:'服务端发起的 Push 流使用偶数 ID（2,4,6...），与客户端发起的奇数 ID 区分', highlight:true },
          { name:'PUSH_PROMISE 时机', value:'收到 HTML 请求后立即', desc:'服务端在发送 HTML 响应的同时，发出 PUSH_PROMISE 通知将推送 CSS/JS', highlight:true },
          { name:'客户端控制', value:'可发 RST_STREAM 拒绝', desc:'客户端若已有缓存，可发 RST_STREAM (0x3) 取消推送，避免带宽浪费', highlight:true },
          { name:'实践现状', value:'Chrome 2022 年已移除', desc:'Chrome 认为收益不足以抵消实现复杂性，103 Early Hints 是更好的替代方案', highlight:false },
        ],
        narration:'服务端推送的价值：传统 HTTP/1.1 中，浏览器先下载 HTML → 解析到 <link rel="stylesheet"> → 再请求 CSS，需要额外一个 RTT。HTTP/2 Server Push 让服务端在客户端发现依赖之前就开始推送，节省一个 RTT。注意：Chrome 已在 2022 年移除了对 HTTP/2 Server Push 的支持，实践中 Early Hints (103) 是更好的替代方案。',
      },
      {
        banner: '✅ HTTP/2 连接建立，多路复用高效传输！',
        leftState: '流量控制中',
        rightState: '优先级调度',
        activeRow: 6,
        fieldTitle: '流控 & 优先级帧字段',
        fields:[
          { name:'WINDOW_UPDATE 帧', value:'类型=0x8', desc:'接收方告知发送方可以再发多少字节（增量），防止接收缓冲区溢出', highlight:true },
          { name:'连接级窗口',       value:'Stream ID=0', desc:'控制整个连接的发送速率', highlight:false },
          { name:'流级窗口',         value:'Stream ID=N', desc:'控制单个流的发送速率，独立于连接级窗口', highlight:false },
          { name:'PRIORITY 帧',     value:'依赖树+权重', desc:'流可依赖另一个流（父子关系），并设置 1~256 的权重，指导服务端资源分配', highlight:true },
          { name:'RST_STREAM 帧',   value:'类型=0x3', desc:'立即终止一个流，不影响其他流——HTTP/1.1 中取消请求只能关闭整个连接', highlight:true },
        ],
        narration:'HTTP/2 的流量控制和优先级系统让连接更智能。流量控制防止快速发送方压垮慢速接收方。优先级让关键资源（如首屏 CSS）优先传输，提升页面加载体验。RST_STREAM 让取消单个请求变得高效——HTTP/1.1 中取消请求只能断开连接，而 HTTP/2 只需发一个 RST_STREAM 帧即可，其他并发流不受影响。',
      },
    ],
    knowledge:[
      { title:'💡 HTTP/2 vs HTTP/1.1 核心改进', body:'① <strong>多路复用</strong>：单连接并发多请求，消除应用层队头阻塞；② <strong>HPACK 头部压缩</strong>：减少 85%+ 重复头部；③ <strong>二进制分帧</strong>：告别 HTTP/1.1 的文本协议，帧处理更高效；④ <strong>服务端推送</strong>：主动推送依赖资源（实践中已逐渐被 Early Hints 替代）；⑤ <strong>流优先级</strong>：关键资源优先传输。' },
      { title:'💡 HTTP/2 仍然存在的问题 → HTTP/3 的动机', body:'HTTP/2 基于 TCP，TCP 本身存在<strong>队头阻塞</strong>：当一个 TCP 数据包丢失，后续所有 HTTP/2 流都会等待重传，即使它们属于不同的流。HTTP/3 将 TCP 替换为 <strong>QUIC</strong>（基于 UDP），每个 QUIC 流独立，丢包只影响对应流，彻底解决了 TCP 层的队头阻塞问题。' },
      { title:'💡 为什么不能用 HTTP/2 明文（h2c）访问网站？', body:'技术上 HTTP/2 支持明文传输（h2c），但主流浏览器（Chrome/Firefox/Safari）均要求 HTTP/2 必须基于 TLS（h2）。原因：① 互联网上存在大量不兼容 HTTP/2 的中间代理，TLS 加密可以避免代理误处理；② 推动 HTTPS 普及。h2c 在内部服务间通信（如 gRPC）中仍有应用。' },
      { title:'💡 HTTP/2 帧类型速查', body:'<strong>DATA (0x0)</strong>：请求/响应正文 | <strong>HEADERS (0x1)</strong>：头部字段 | <strong>PRIORITY (0x2)</strong>：优先级 | <strong>RST_STREAM (0x3)</strong>：重置流 | <strong>SETTINGS (0x4)</strong>：参数设置 | <strong>PUSH_PROMISE (0x5)</strong>：服务端推送预告 | <strong>PING (0x6)</strong>：心跳 | <strong>GOAWAY (0x7)</strong>：关闭连接 | <strong>WINDOW_UPDATE (0x8)</strong>：流量控制' },
      { title:'💡 Server Push 为何退出历史舞台？', body:'Server Push 存在三大问题：① <strong>缓存感知差</strong>：服务端无法知道客户端是否已有缓存，可能浪费带宽推送重复资源；② <strong>时序难以控制</strong>：Push 可能在 Service Worker 拦截之前到达，造成竞争；③ <strong>实现复杂</strong>。Chrome 于 2022 年移除了 HTTP/2 Server Push 支持，<strong>103 Early Hints</strong> 已成为更好的替代方案。' },
      { title:'💡 HTTP/2 流控与优先级', body:'<strong>流量控制</strong>：HTTP/2 在连接级和流级都有独立的流量控制窗口（WINDOW_UPDATE 帧），防止快速发送方压垮慢速接收方。<strong>优先级</strong>：PRIORITY 帧可设置流依赖树和权重（1~256），让关键资源（如首屏 CSS）优先传输。<strong>RST_STREAM</strong>：单独取消一个流，无需断开整个连接——这是 HTTP/1.1 做不到的。' },
    ],
    quiz:[
      { q:'HTTP/2 如何解决 HTTP/1.1 的队头阻塞？底层是否还有队头阻塞？', a:'HTTP/2 通过<strong>多路复用</strong>解决了应用层的队头阻塞：多个请求被分配到不同的流，帧可交错传输，一个流的延迟不影响其他流。但 HTTP/2 基于 TCP，<strong>TCP 层仍存在队头阻塞</strong>：若某个 TCP 数据包丢失，后续所有数据（不论属于哪个 HTTP/2 流）都必须等待重传。HTTP/3 用 QUIC（基于 UDP）彻底解决了传输层队头阻塞。' },
      { q:'HPACK 的动态表是如何工作的？两端如何保持同步？', a:'动态表是 HPACK 编码器（发送方）和解码器（接收方）各自维护的相同有序列表。发送方将新出现的头部字段加入动态表，后续可用索引号引用。两端通过处理相同的 HEADERS 帧序列来<strong>自动保持同步</strong>。接收方可通过 SETTINGS_HEADER_TABLE_SIZE 限制动态表大小，发送方必须遵守（驱逐旧条目）。' },
      { q:'Server Push 在实践中为什么逐渐被放弃？', a:'Server Push 的主要问题：① <strong>缓存感知差</strong>：服务端不知道客户端是否已有缓存，可能浪费带宽；② <strong>RTT 时序难以控制</strong>：Push 可能在 Service Worker 拦截之前就到达，导致竞争；③ <strong>实现复杂</strong>：浏览器和服务器正确处理 Push 的难度高。Chrome 2022 年移除了对 HTTP/2 Server Push 的支持，<strong>103 Early Hints</strong> 是更好的替代。' },
      { q:'如何判断一个网站是否使用了 HTTP/2？', a:'① <strong>浏览器开发者工具</strong>：Network 面板 → 右键列头 → 勾选 "Protocol"，h2 = HTTP/2；② <strong>curl 命令</strong>：`curl -I --http2 https://example.com -v` 查看输出中的 "Using HTTP2"；③ 在线工具：KeyCDN HTTP/2 Test、SSL Labs 等。' },
    ],
  };
