protocolDB['iproute'] = {
  /* 动画包（左→右=出发，右→左=回程，这里全程单向） */
  rows: [
    { dir:'r', color:'packet-label-blue',   label:'IP 数据包（src=192.168.1.10 → dst=10.0.3.5）' },
    { dir:'r', color:'packet-label-blue',   label:'IP 数据包（最长前缀匹配 → 默认路由 0.0.0.0/0）' },
    { dir:'r', color:'packet-label-green',  label:'IP 数据包（TTL=63，到达 R1，查转发表）' },
    { dir:'r', color:'packet-label-purple', label:'IP 数据包（TTL=62，到达 R2，再次查转发表）' },
    { dir:'r', color:'packet-label-green',  label:'IP 数据包（直连网络，ARP 获取 MAC）' },
    { dir:'r', color:'packet-label-blue',   label:'IP 数据包（TTL=61，到达目标主机）' },
  ],
  steps: [
    { title:'主机发包，查路由表', emoji:'📤' },
    { title:'最长前缀匹配', emoji:'🔍' },
    { title:'路由器 R1 转发', emoji:'🔀' },
    { title:'路由器 R2 转发', emoji:'🔀' },
    { title:'直连网络投递', emoji:'📡' },
    { title:'到达目标主机', emoji:'✅' },
  ],
  stepData: [
    {
      banner:'📤 第 1 / 6 步 — 主机 192.168.1.10 发出数据包',
      leftState:'准备发送', rightState:'等待接收',
      fields:[
        { name:'源 IP',           value:'192.168.1.10', desc:'发送方主机的 IP 地址' },
        { name:'目标 IP',         value:'10.0.3.5',     desc:'接收方主机的 IP 地址' },
        { name:'TTL（生存时间）', value:'64', desc:'每经过一台路由器 TTL -1，归零则丢弃并返回 ICMP Time Exceeded', highlight:true },
        { name:'协议字段',        value:'6（TCP）',     desc:'上层协议类型：1=ICMP，6=TCP，17=UDP' },
        { name:'路由表查询',      value:'目标不在本网段', desc:'192.168.1.10/24 与 10.0.3.5 不在同一 /24，触发默认路由' },
        { name:'下一跳',          value:'192.168.1.1（默认网关）', desc:'主机将数据包发给默认网关，由路由器代为转发', highlight:true },
      ],
      narration:'主机发送 IP 数据包前先查询自己的路由表。目标地址 10.0.3.5 不在本地子网（192.168.1.0/24），命中默认路由（0.0.0.0/0），数据包被送往默认网关 192.168.1.1。此时用 ARP 获取网关 MAC 地址，封装成以太网帧发出。',
    },
    {
      banner:'🔍 第 2 / 6 步 — 最长前缀匹配（Longest Prefix Match）',
      leftState:'发包中', rightState:'等待接收',
      fields:[
        { name:'目标 IP',         value:'10.0.3.5',  desc:'待匹配的目标地址' },
        { name:'路由条目 1',      value:'10.0.0.0/8  → R1',  desc:'匹配长度 8 位（0.0.0.0~10.255.255.255）' },
        { name:'路由条目 2',      value:'10.0.3.0/24 → R2',  desc:'匹配长度 24 位，更精确', highlight:true },
        { name:'路由条目 3',      value:'0.0.0.0/0   → R1',  desc:'默认路由，最后兜底' },
        { name:'最终选择',        value:'10.0.3.0/24（匹配 24 位）', desc:'最长前缀匹配原则：选前缀最长（最精确）的那条路由', highlight:true },
        { name:'下一跳',          value:'R2（172.16.0.2）', desc:'由最长匹配的路由条目决定下一跳' },
      ],
      narration:'路由器转发数据包时采用"最长前缀匹配"原则（Longest Prefix Match）：在所有匹配目标 IP 的路由条目中，选择前缀长度最长的（即最精确的那条）。本例 10.0.3.0/24 比 10.0.0.0/8 更精确，被选中。这是 IP 路由的核心决策算法。',
    },
    {
      banner:'🔀 第 3 / 6 步 — 路由器 R1 收包并转发',
      leftState:'包到达 R1', rightState:'等待接收',
      fields:[
        { name:'入接口',          value:'eth0（192.168.1.1/24）', desc:'R1 从 eth0 接口收到数据包' },
        { name:'TTL 检查',        value:'64 → 63（-1）', desc:'路由器每转发一次必须将 TTL 减 1，防止包无限循环', highlight:true },
        { name:'目标 IP 查找',    value:'10.0.3.5 → 命中 10.0.0.0/8', desc:'R1 路由表中命中 10.0.0.0/8 的路由' },
        { name:'下一跳',          value:'172.16.0.2（R2）',       desc:'R1 知道 10.0.x.x 网段需要发给 R2' },
        { name:'出接口',          value:'eth1（172.16.0.1/30）',  desc:'根据下一跳地址选择出接口' },
        { name:'封装新以太网帧',  value:'src MAC=R1-eth1，dst MAC=R2-eth1', desc:'IP 地址不变，MAC 地址每跳都重新封装', highlight:true },
      ],
      narration:'R1 收到数据包后：① TTL -1（64→63），若 TTL=0 则丢弃并发 ICMP Time Exceeded；② 查路由表选下一跳 R2；③ 剥掉旧以太网帧头，重新封装新的帧（目标 MAC = R2 的 MAC）。注意：IP 头始终不变（src/dst IP 不变），只有以太网帧头在每一跳重新封装。',
    },
    {
      banner:'🔀 第 4 / 6 步 — 路由器 R2 收包并转发',
      leftState:'包到达 R2', rightState:'等待接收',
      fields:[
        { name:'入接口',          value:'eth0（172.16.0.2/30）',  desc:'R2 从 eth0 接口收到来自 R1 的数据包' },
        { name:'TTL 检查',        value:'63 → 62（-1）', desc:'R2 同样将 TTL -1', highlight:true },
        { name:'目标 IP 查找',    value:'10.0.3.5 → 命中 10.0.3.0/24', desc:'R2 路由表中有直连路由 10.0.3.0/24（直连网段）', highlight:true },
        { name:'路由类型',        value:'直连路由（Connected）', desc:'目标网段直接连在 R2 的 eth1 接口上，无需再跳' },
        { name:'出接口',          value:'eth1（10.0.3.1/24）',    desc:'直接从 eth1 发出，目标主机就在这个子网里' },
        { name:'ARP 解析',        value:'查询 10.0.3.5 的 MAC 地址', desc:'目标在直连网段，需 ARP 广播获取目标 MAC 才能封帧' },
      ],
      narration:'R2 是最后一台路由器。目标网段 10.0.3.0/24 是 R2 的直连接口（eth1 的子网），这意味着目标主机就在 R2 的邻居网络里。R2 发出 ARP 广播，询问"谁是 10.0.3.5？"，目标主机回复自己的 MAC，R2 据此封装最终的以太网帧。',
    },
    {
      banner:'📡 第 5 / 6 步 — 直连网络最终投递（ARP + 帧封装）',
      leftState:'包到达目标网络', rightState:'等待接收',
      fields:[
        { name:'ARP 广播',        value:'Who has 10.0.3.5?（全广播 FF:FF:FF:FF:FF:FF）', desc:'R2 在直连网络广播 ARP 请求' },
        { name:'ARP 回复',        value:'10.0.3.5 is at aa:bb:cc:dd:ee:ff', desc:'目标主机收到广播后回复自己的 MAC' },
        { name:'以太网帧封装',    value:'dst MAC = aa:bb:cc:dd:ee:ff',      desc:'R2 用目标 MAC 封装最终帧，准备发出', highlight:true },
        { name:'IP 头不变',       value:'src=192.168.1.10，dst=10.0.3.5',   desc:'整个路由过程 IP 地址始终不变，MAC 地址每跳替换' },
        { name:'TTL',             value:'62', desc:'最终到达时 TTL=62，经过了 2 台路由器（64-2=62）' },
        { name:'最终发出',        value:'帧发往 10.0.3.5（直连交付）', desc:'帧通过以太网直接交付到目标主机，无需再路由', highlight:true },
      ],
      narration:'ARP 解析是路由的最后一步——仅在同网段（直连投递）时才需要。ARP 广播只在本地网段传播（路由器不转发广播），目标主机应答后，R2 获得目标 MAC，完成以太网帧封装，数据包成功发出。',
    },
    {
      banner:'✅ 第 6 / 6 步 — 数据包到达目标主机',
      leftState:'发送完成', rightState:'已接收',
      fields:[
        { name:'目标主机',        value:'10.0.3.5', desc:'成功收到来自 192.168.1.10 的数据包' },
        { name:'路径跳数',        value:'2 跳（经过 R1 → R2）', desc:'TTL 从 64 降到 62，减少了 2，说明经过了 2 台路由器' },
        { name:'IP 头完整性',     value:'src=192.168.1.10，dst=10.0.3.5（未变）', desc:'IP 地址在整个路由过程中保持不变，这是 IP 协议的核心特性', highlight:true },
        { name:'MAC 历程',        value:'主机→R1→R2→目标（每跳替换）', desc:'以太网帧的 MAC 在每一跳重新封装，只在本地链路有意义', highlight:true },
        { name:'协议模型',        value:'IP 负责端到端寻址，MAC 负责逐跳寻址', desc:'IP=全球地址（不变），MAC=本地地址（逐跳更换），两层协作' },
        { name:'路由核心总结',    value:'路由表 + 最长前缀匹配 + TTL -1 + 逐跳封帧', desc:'这四个机制共同构成了互联网 IP 路由的工作原理' },
      ],
      narration:'数据包完成了从 192.168.1.10 到 10.0.3.5 的跨网段传输。整个过程体现了 IP 路由的核心哲学：IP 地址（逻辑地址）负责端到端的全局寻址，始终不变；MAC 地址（物理地址）只在相邻节点间有意义，每跳重新封装。路由器的职责就是根据路由表，把数据包一跳一跳地转发到目标。',
    },
  ],

  knowledge: [
    { label:'路由表',       value:'路由器/主机中存储"目标网段 → 下一跳/出接口"的映射表。条目来源：直连路由（直接配置的接口）、静态路由（手动配置）、动态路由（OSPF/BGP 自动学习）' },
    { label:'最长前缀匹配', value:'LPM（Longest Prefix Match）：匹配目标 IP 的所有路由条目中，选前缀最长（掩码最长）的那条。例：10.0.3.5 同时匹配 10.0.0.0/8 和 10.0.3.0/24，选 /24（更精确）' },
    { label:'TTL',          value:'Time-To-Live（生存时间）：每经过一台路由器 -1，归零丢弃并返回 ICMP Type 11（Time Exceeded）。防止数据包在路由环路中无限循环。Linux/macOS 初始 TTL=64，Windows=128' },
    { label:'IP vs MAC',    value:'IP 地址（逻辑地址）负责全局端到端寻址，在整个路由过程中不变；MAC 地址（物理地址）只在同一链路/网段有意义，每跳重新封装。这是 IP 路由的核心两层分工' },
    { label:'下一跳',       value:'Next Hop：路由器将数据包转发给的邻居 IP 地址。直连路由的下一跳是"直接投递"；其他路由的下一跳是下一台路由器的接口 IP' },
    { label:'路由协议',     value:'动态路由协议自动更新路由表：IGP（域内）：OSPF（链路状态）、EIGRP；EGP（域间）：BGP（互联网骨干，全球约 100 万条路由）。静态路由适合小型固定拓扑' },
  ],

  quiz: [
    { q:'IP 路由转发时，哪个字段会在每经过一台路由器后减 1？',
      options:['源 IP 地址','TTL（生存时间）','目标 MAC 地址','协议字段'],
      answer: 1,
      exp:'TTL（Time-To-Live）每经过一台路由器减 1，到 0 时路由器丢弃数据包并返回 ICMP Type 11（Time Exceeded）报文，防止数据包在路由环路中无限循环。Traceroute 工具就是利用这个机制逐跳探测路径。' },
    { q:'最长前缀匹配（LPM）的含义是什么？',
      options:['选择路由表中跳数最少的路由','选择前缀长度最长（掩码最具体）的匹配路由','选择最新添加的路由','随机选择一条匹配的路由'],
      answer: 1,
      exp:'LPM = Longest Prefix Match：在所有匹配目标 IP 的路由条目中，选掩码最长（前缀位数最多）的那条。掩码越长，路由越精确。例如 10.0.3.0/24 比 10.0.0.0/8 更精确，优先选择 /24。这是 IP 转发的核心算法。' },
    { q:'数据包在路由过程中，哪个地址会每跳改变，哪个始终不变？',
      options:['IP 地址每跳改变，MAC 地址不变','两者都不变','IP 地址不变，MAC 地址每跳改变','两者都改变'],
      answer: 2,
      exp:'IP 地址（逻辑地址）负责全局寻址，整个路由过程中 src/dst IP 不变。MAC 地址（物理/链路层地址）只在相邻设备间有意义，路由器每次转发都剥掉旧帧头、重新封装新帧（目标 MAC = 下一跳的 MAC），所以 MAC 地址每跳都改变。' },
    { q:'路由器转发数据包时，如果 TTL 变为 0 会怎样？',
      options:['继续转发但标记为低优先级','丢弃数据包，并向源主机发送 ICMP Time Exceeded','重置 TTL 为 64 继续转发','直接丢弃，不通知源主机'],
      answer: 1,
      exp:'TTL 归零时，路由器丢弃该数据包，并向原始发送方返回 ICMP Type 11（Time Exceeded）报文，告知"数据包因 TTL 耗尽被丢弃"。Traceroute 工具正是利用这一机制：依次发送 TTL=1、2、3…的探测包，收集每跳返回的 ICMP 报文，绘制出完整路径。' },
    { q:'在路由器的直连网络（Connected Route）上，如何获取目标主机的 MAC 地址？',
      options:['查询路由表','发送 ARP 广播请求','查询 DNS 服务器','通过 ICMP 探测'],
      answer: 1,
      exp:'目标主机在直连网段（同一广播域）时，路由器通过 ARP（地址解析协议）广播"Who has 10.0.3.5？"，目标主机回复自己的 MAC 地址，路由器据此封装最终的以太网帧。ARP 广播只在本地网段传播，路由器不会转发广播。' },
  ],
};
